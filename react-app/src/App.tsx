import Button from "./components/Button";

const App = () => {
  return (
    <>
      {/* <MyButton text="Click Me" onClick={() => alert("hello world")} />

      <MyButton text={12234} onClick={() => alert("hello world")} />

      <MyButton text={true} onClick={()=>alert('hello world')}/> */}

      <Button text="Click Me"/>

</>
  );
};

export default App;
